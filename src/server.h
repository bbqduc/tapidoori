#pragma once

#include <packets.h>
#include <iostream>

class TapidooriServer
{
	template<typename T>
	void HandlePacket(const T& packet);
};


void TapidooriServer::<MessagePacket>(const MessagePacket& packet)
{
	std::cout << "
}
